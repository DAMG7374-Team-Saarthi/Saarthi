define flow format as json
  $formatted = (format json from $input)
  bot: $formatted

define subflow format json
  if $input is json
    return $input
  else
    $json = (convert $input to json)
    return $json